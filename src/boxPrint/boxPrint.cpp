#include <string>
#include <iostream>
#include <vector>

using namespace std;
void boxPrint(const vector<string>& text  ){
    auto length = text.length();
    case
}
};

auto main() -> int {
    auto text = string();
    cout << "Let's print! enter text!: ";
    cin >> text;
    cout << "Choose a symbol to decorate your text";
    auto xchar = char();
    cin >> char;
    boxPrint ({text}, xchar);
}